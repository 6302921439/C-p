#include <stdio.h>

int main() {
    int n, element, count = 0;
    printf("Enter the size of the array: ");
    scanf("%d", &n);

    int arr[n];

    // Input elements of the array
    printf("Enter the elements of the array: ");
    for (int i = 0; i < n; i++) {
        scanf("%d", &arr[i]);
    }

    // Input the element to be searched
    printf("Enter the element to be searched: ");
    scanf("%d", &element);

    // Count the occurrence of the element in the array
    for (int i = 0; i < n; i++) {
        if (arr[i] == element) {
            count++;
        }
    }

    // Print the occurrence of the element in the array
    printf("The occurrence of %d in the array is %d\n", element, count);

    return 0;
}

Explanation:

    We start by asking the user to enter the size of the array and the elements of the array.
    We declare an integer array arr of size n to store the input elements of the array.
    We use a for loop to input each element of the array from the user. We store the input elements in arr.
    We ask the user to input the element to be searched.
    We declare an integer variable count and initialize it to zero.


